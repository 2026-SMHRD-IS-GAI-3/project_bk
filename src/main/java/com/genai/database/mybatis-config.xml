<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
 
<!-- configuration : mybatis 설정을 기술하는 태그 -->
<configuration>

	<properties resource="com/genai/database/db.properties"></properties>
	
	<typeAliases>
		<typeAlias type="com.genai.model." alias=""/>
		<typeAlias type="com.genai.model." alias=""/>
	</typeAliases>
	
  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>
  </environments>
  
  <!-- 사용자가 수행할 sql문장을 파일로 연결하여 관리하는 태그 -->
  <mappers>
    <mapper resource="com/genai/database/Mapper.xml"/>
    <mapper resource="com/genai/database/Mapper.xml"/>
  </mappers>
</configuration>